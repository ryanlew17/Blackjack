# design — 模块行为设计快照

> 适用范围：各模块**当前已实现的稳定行为**——模块是什么、行为规则是什么。不存放增量需求（→ `../requirements/`）与存档字段细节（→ `../contracts/`）。
> 何时读取：按根 AGENTS.md 路由按需读取对应模块文档；不要默认全量读取。
> 关联需求：无（本目录为当前实现快照）。
> 最后更新：2026-09-22
> 来源：2026-09-22 文档架构升级新建（原 README 规则章节 + 重构定稿行为）

## 文档索引

| 文档                                               | 模块                                                 | 何时读取                                              |
| -------------------------------------------------- | ---------------------------------------------------- | ----------------------------------------------------- |
| [game-rules.md](./game-rules.md)                   | 规则引擎：RuleSet、阶段机、命令、结算、事件          | 改 `src/domain/`、赔付、规则变体                      |
| [state-and-animation.md](./state-and-animation.md) | useGame：权威状态、动画队列、刷新恢复、跨标签冲突    | 改 `src/application/useGame.ts`、动画节奏、持久化时机 |
| [persistence.md](./persistence.md)                 | 存档生命周期：自动保存、校验入口、导入确认、损坏恢复 | 改 `src/infrastructure/save.ts`、导入导出流程         |
| [ui.md](./ui.md)                                   | 界面：组件结构、i18n、响应式、动效与音效约定         | 改 `src/presentation/` 任何组件、样式、文案           |

## 与其他文档的边界

- **存档字段、版本、校验规则** → `../contracts/save-format.md`（本目录只描述行为与流程）。
- **宏观产品（定位/用户/非目标/模块地图）** → `../prd.md`。
- **技术选型、分层、目录结构、构建** → `../architecture.md`。
- 行为变更类需求 → `../requirements/active/`，落地后回写本目录快照。
