# 测试与验证门槛

> 适用范围：提交/发布前的质量门槛——单元测试、类型检查、格式、构建、浏览器验收；以及历次验收记录与已知边界。
> 何时读取：写完代码准备验证时；发布前；核对某平台是否验收过时。
> 关联需求：无（门槛本身的需求变更走 `requirements/`）。
> 最后更新：2026-09-22
> 来源：原 docs/VERIFICATION.md（2026-09-22 迁入并改现名）

## 1. 自动检查（每次改动）

```bash
npm test              # Vitest：规则与存档校验（当前 60 项，含 300 局牌组检查与 600 局固定种子逐决策存档往返）
npm run typecheck     # 严格 TypeScript
npm run format:check  # Prettier（源码、docs、双语 README、AGENTS.md 等；启动脚本与 LICENSE 不在其列）
npm run build         # tsc -b + vite build → dist/
```

规则测试覆盖：多 A 软硬转换、天然双方平局、庄家天然、3:2 赔率、6:5 赔率变体、普通 21 点继续等待庄家、软 17 停牌、软 17 补牌变体、庄家补牌、双方爆牌、加倍、加倍余额不足、要牌后不可加倍、重复结算防护、非法下注、撤销清空、半筹码全押、不可变状态、下一局及事件顺序。

存档测试覆盖：下注中/行动中/已结算状态往返、加倍后状态、损坏/旧版/未来版本/超限文件、重复/缺失/越界牌张、非法金额/阶段/偏好、伪造结果与历史、存储读取失败、写入失败，以及损坏自动存档不会自动覆盖。

## 2. 浏览器验收（UI 改动 / 发布前）

**v1 通过记录见下表。规则 v2（分牌/保险/晚投降）已于 2026-09-22 完成独立最终验收：Chromium 全场景 100 项、Firefox 93 项（同日补验）、WebKit 子集 53 项全部通过；详见 [v2 验收报告](./requirements/v2-acceptance-report.md)。**

使用 Playwright CLI 执行，未引入第二套测试框架；先 `npm run build` 用生产产物走查。

| 项目                                                  | 结果                              |
| ----------------------------------------------------- | --------------------------------- |
| Chrome 完整下注、发牌、要牌、停牌、结算、下一局、刷新 | 通过                              |
| WebKit 下注、加倍、结算、刷新恢复                     | 通过                              |
| 中文切换、静音、减少动态效果及刷新持久化              | 通过                              |
| 320px 宽度中英文布局无横向溢出                        | 通过                              |
| 390px 中文与桌面视觉截图检查                          | 通过                              |
| 下载真实存档、导入备份并确认替换                      | 通过                              |
| 无效文件提示，保持原存档不变                          | 通过                              |
| 第二个标签页修改后暂停，载入最新进度                  | 通过                              |
| 连续两次发牌/停牌命令只生效一次                       | 通过                              |
| 发牌/结算动画期间刷新，状态与资金不重复处理           | 通过                              |
| 模拟页面恢复可见，结束滞后的动画队列                  | 通过                              |
| 原生弹窗焦点限制与 Esc 关闭                           | 通过                              |
| 浏览器存储被禁用时显示提醒并继续游戏                  | 通过                              |
| Firefox                                               | 补验通过（2026-09-22，见第 7 节） |

Firefox 曾于 2026-09-21 安装并尝试默认临时配置、独立配置目录与有界面模式：前两者报告 `Could not find profile folder`，有界面模式报告 macOS `sandbox_extension_issue_file_to_process ... Operation not permitted` 并启动超时；当时未计为通过。2026-09-22 用户在本机安装 Firefox 156 后，Playwright 捆绑版 Firefox 155 可正常启动，遂以 v2 基础矩阵（93 项；额外 7 项的复核仍有环境阻塞，详见验收报告）补验通过；390px 视觉截图一项沿用 Chrome/WebKit 既有记录，未在 Firefox 单独重跑。

2026-09-22 重构后桌面浏览器复验（Kimi 桌面浏览器）：完整对局（下注、发牌、要牌后 Double 正确禁用、停牌、结算、下一局，连续两局）、进行中刷新精确恢复、五个面板开合与 Esc 关闭、历史记录渲染、中文界面、320px 宽度无横向溢出，均通过。导入预览「关闭后重开无残留」因该浏览器工具不支持文件选择未能覆盖；对应逻辑（`closePanel` 清空 `pending`）已经代码审查核对。

截图及测试下载文件位于 `output/playwright/`（已忽略，不属于发布内容）。

## 3. 生产产物复验

在 `npm run preview` 的静态生产版本上，使用全新 Chrome 会话完成下注、发牌、要牌、停牌、结算与下一局。固定牌序下平局正确返还本金，余额恢复 2,000，未捕获运行时错误。

## 4. 已知边界

- 当前仍为单机、虚拟筹码；分牌、保险、晚投降已通过最终验收（2026-09-22），尚未发布；无云端功能。
- 自动保存依赖浏览器站点数据；存储不可用时需要手动导出文件。
- 兼容性使用桌面浏览器及移动视口验证，未使用真实 iOS / Android 设备。
- 音效调用与静音偏好已验证；没有使用录音设备进行听感验收。
- 尚未发布线上站点，部署产物在 `dist/`。
- `start-game.bat`（Windows）尚未在真实 Windows 机器上实测；`start-game.command` 已在 macOS 实测。

## 5. 验收记录（历史）

本地验证日期：2026-09-21。环境：macOS arm64，Node.js 26.9.0，npm 12.0.2。TypeScript 严格类型检查通过；Vitest 30 项通过；Vite 生产构建通过（JS gzip 约 124 KB，CSS gzip 约 5.3 KB）；npm 依赖审计 0 个已知漏洞。

2026-09-22 修订：

- 存档校验取消余额与下注总量 10^12 的固定上限，改为与游戏逻辑一致的非负安全整数约束，任何可达状态（含超大余额）均可导出后重新导入。Vitest 增至 31 项并全部通过；类型检查、格式检查与生产构建同步通过。
- 代码结构优化：`App.tsx` 拆分为 `presentation/components/`（Icon/Card/Chip/Modal/GameTable/ControlDeck 与 panels/ 五个面板），新增 `presentation/format.ts`；规则逻辑参数化——`standSoft17` 与 `blackjackPayout` 经 `transition` 的可选 `rules` 参数接线（ADR-001），新增 6:5 赔率与软 17 补牌两项测试；`effectId` 改为单调计数器，`fileError` 类型收紧。Vitest 增至 33 项并全部通过；类型/格式/构建通过。
- 冒烟发现并修复一个存量缺陷：`.dealer-zone`（`top: 30px` 的绝对定位层）遮挡 `.table-meta` 的历史按钮导致鼠标无法点击，为 `.table-meta` 增加 `z-index: 1`（无视觉变化）。修复前后均在浏览器实测：修复前点击无响应，修复后面板正常打开。

## 6. 规则 v2 开发自测（非最终验收，2026-09-22）

- Vitest 60 项：新增分牌、分 A、分后加倍、混合结果、保险天然组合、晚投降、版本拒绝与伪造存档测试；600 局固定随机种子在每个决策点校验存档往返及 30 局历史截断。
- 内置浏览器仅做开发冒烟：320px 中文导入 v2 固定牌序存档、分牌、第一手停牌后转第二手、刷新保留该状态。发现并修复状态标签与下注区重叠；修正后页面 scrollWidth = viewport = 320，手牌顶端在下注区底端下方 47px。
- Playwright CLI 启动受 npm registry DNS 失败阻塞；内置浏览器冒烟不等同于 Chrome/WebKit/Firefox 最终验收。尚未对 v2 完成生产产物浏览器矩阵、英文窄屏、保险/投降完整 UI 流程或跨标签最终验收。
- 最终验收、需求归档与发布均未执行。交接 Agent 须以当前代码独立复核，不得沿用本节冒烟作为最终通过结论。

## 7. 规则 v2 最终验收（2026-09-22，通过）

由独立验收 Agent 依据 [交接清单](./requirements/v2-handoff.md) 执行，完整证据见 [v2 验收报告](./requirements/v2-acceptance-report.md)：

- 质量门槛复跑全绿：Vitest 60/60、类型、格式、`git diff --check`、生产构建（Vite 6.4.3）。
- 生产 `dist/` + `npm run preview`，固定牌序存档夹具导入断言精确金额：Chromium（Playwright 1.63）100/100——分牌（同点值/分 A/分牌 21/分后加倍/混合输赢/双重点击防护/第二手刷新恢复）、保险（买入/放弃/余额恰够与不足/双方天然组合/暗牌不泄露/重复购买防护/决策中刷新）、晚投降（合法与非法时机/一筹码半注/保险损失保留）、存档（真实导出导入/损坏/v1/v3/伪造拒绝/旧档版本阻止与写入封锁/跨标签双向冲突/存储禁用降级）、320px 中英文无横向溢出、原生键盘与弹层焦点、减少动态效果、模拟页面恢复可见，全程零控制台/页面错误。
- WebKit 26.6 子集 53/53 通过；Firefox 基础矩阵有 93/93 成功报告。额外 7 项缺少成功证据，本轮补跑因配置档启动失败阻塞，未计入通过数。
- 结论：零产品缺陷，REQ-2026-003–006 验收通过并归档；未提交、未推送、未发布。

提交前证据复核：Firefox 可确认的落盘证据为 93 项，额外 7 项本轮补跑因 `Could not find profile folder` 阻塞；原 100 项结论已更正，详见验收报告第 8 节。
