# 测试与验证门槛

> 适用范围：提交/发布前的质量门槛——单元测试、类型检查、格式、构建、浏览器验收；当前覆盖状态、已知边界与验证历史摘要。
> 何时读取：写完代码准备验证时；发布前；核对某平台是否验收过时。
> 关联需求：无（门槛本身的需求变更走 `requirements/`）。
> 最后更新：2026-09-22
> 来源：原 docs/VERIFICATION.md（2026-09-22 迁入并改现名；2026-09-22 随规则 v2 验收完成整理）

## 1. 自动检查（每次改动）

```bash
npm test              # Vitest：规则与存档校验（当前 63 项，含 300 局牌组检查与 600 局固定种子逐决策存档往返）
npm run typecheck     # 严格 TypeScript
npm run format:check  # Prettier（源码、docs、双语 README、AGENTS.md 等；启动脚本与 LICENSE 不在其列）
npm run build         # tsc -b + vite build → dist/
```

规则测试覆盖：多 A 软硬转换、天然双方平局、庄家天然、3:2 赔率、6:5 赔率变体、普通 21 点继续等待庄家、软 17 停牌、软 17 补牌变体（补牌爆牌、A 降级硬 17、多 A 软 17）、庄家补牌、双方爆牌、加倍、加倍余额不足、要牌后不可加倍、重复结算防护、非法下注、撤销清空、半筹码全押、不可变状态、下一局及事件顺序；分牌（同点值/不同十点牌/余额不足/行动后不可分/仅一次、分 A、分牌 21 非天然、分后加倍、混合结果、双爆牌、庄家统一行动）、保险（明牌 A/非 A、购买/放弃、余额恰够/不足、玩家天然 × 庄家天然 × 购买组合、锁阶段动作）、晚投降（合法首轮、要牌/加倍/分牌后/庄家天然/保险决策中不可用、奇数筹码半注、保险损失保留）。

存档测试覆盖：下注中/行动中/已结算状态往返、分牌与保险各决策点往返、加倍后状态、损坏/旧版/未来版本/超限文件、重复/缺失/越界牌张、非法金额/阶段/偏好、伪造活动手/手牌数/保险/结果/历史、存储读取失败、写入失败、损坏与版本不符的自动存档不自动覆盖、超大余额与不可表示金额转换拒绝。

## 2. 浏览器验收（UI 改动 / 发布前）

规范：使用 Playwright CLI（未引入第二套测试框架），先 `npm run build` 以生产产物走查（`npm run preview`）；仅记录实际覆盖，环境阻塞不计为通过。固定牌序存档夹具导入后抽牌顺序确定，金额可精确断言；截图与夹具位于 `output/playwright/`（已忽略，不属发布内容）。

当前覆盖（规则 v2 最终验收，2026-09-22，结论与证据见 `requirements/archive/` 各需求文档的"验收记录"小节）：

| 引擎                               | 覆盖                                            |
| ---------------------------------- | ----------------------------------------------- |
| Chromium（Playwright 1.63）        | 100/100，零控制台/页面错误                      |
| WebKit 26.6                        | 代表性子集 53/53，零控制台错误                  |
| Firefox（Playwright 捆绑版 155.0） | 基础矩阵 93/93，零控制台错误；另 7 项见已知边界 |

v2 全场景清单（Chromium 全量、Firefox 基础矩阵同集）：

| 场景组                                                                                                                               | 结果 |
| ------------------------------------------------------------------------------------------------------------------------------------ | ---- |
| 分牌全流程：导入→分牌（双击仅扣一次）→两手轮转→混合结算（+10/2,010）→历史逐手明细→下一局                                             | 通过 |
| 分 A（自动停牌、双 21 按 1:1 共 +20）、分牌 21 自动停牌（win/lose 净 0）                                                             | 通过 |
| 分牌后加倍（实际点击：加倍注 20、单张停牌、双手输净 -30 精确一次）                                                                   | 通过 |
| 第二手行动中刷新：精确恢复、暗牌仍隐藏；发牌双击防护（仅发一次）                                                                     | 通过 |
| 保险：买入（扣 5 一次、暗牌不泄露）、放弃、余额不足（买入禁用+提示）、双方天然（平局+保险赢 +1）、仅玩家天然（3:2 +1.5）、决策中刷新 | 通过 |
| 晚投降：合法时机、要牌后禁用、分牌后不可用、奇数筹码半注返还（1,998.5 / 净 -1.5）、保险损失保留（净 -10）                            | 通过 |
| 庄家补牌动画中刷新：结算 push、余额 2,000 精确、历史仅一条、庄家 4 张                                                                | 通过 |
| 真实导出（下载事件捕获文件、v2 信封、权威状态）→全新配置档导入替换                                                                   | 通过 |
| 负样本导入：损坏 / v1 / v3 / 伪造历史——均拒绝且进度与存储不变                                                                        | 通过 |
| 旧档版本：横幅提示、动作与设置写入阻止（存储原文不变）、明确重开生效、导入 v2 后解除                                                 | 通过 |
| 跨标签冲突：双向（A↔B）暂停 + 载入最新恢复                                                                                           | 通过 |
| 键盘：Tab 聚焦筹码 Enter 下注；弹层内无可达外部控件（原生 inert）、Esc 关闭                                                          | 通过 |
| 减少动态效果：设置后整局快速结算且结果一致                                                                                           | 通过 |
| 320px 中英文：分牌双手/保险/已结算/历史面板 scrollWidth ≤ 320，双手与标签完整                                                        | 通过 |
| 存储禁用（localStorage 抛错）：提醒显示、内存会话继续可下注发牌、无页面错误（仅 Chromium 有落盘证据）                                | 通过 |
| 页面恢复可见（模拟后台节流后返回）：结束滞后动画队列、落定已提交结算、无重复赔付（仅 Chromium 有落盘证据）                           | 通过 |

生产产物复验：固定牌序下平局正确返还本金、余额恢复 2,000，未捕获运行时错误（v1 Chrome 全新会话与 v2 Chromium/Firefox 矩阵均覆盖）。

## 3. 已知边界

- Firefox：基础 93 项有成功证据；另有 7 项增补场景（分牌后加倍、存储禁用、页面恢复可见）因本机 Firefox 启动间歇性失败（`Could not find profile folder`）未取得通过证据，环境稳定后可补验。390px 视觉截图仅在 Chrome/WebKit 覆盖。
- 导入路径以 Playwright `setInputFiles` 驱动（等价真实文件选择）；导出经真实下载事件捕获文件内容验证，未验证浏览器另存对话框本身。
- 兼容性使用桌面浏览器及移动视口验证，未使用真实 iOS / Android 设备。
- 音效调用与静音偏好已验证；没有使用录音设备进行听感验收。
- 自动保存依赖浏览器站点数据；存储不可用时需要手动导出文件。当前为单机、虚拟筹码，无云端功能。
- `start-game.bat`（Windows）尚未在真实 Windows 机器上实测；`start-game.command` 已在 macOS 实测。

## 4. 验证历史（摘要）

- 2026-09-21 v1 初验：macOS arm64、Node 26.9.0；TS 严格通过、Vitest 30 项、生产构建通过（JS gzip 约 124 KB）、依赖审计 0 漏洞。
- 2026-09-22 v1 修订：存档校验取消 10^12 固定上限改为非负安全整数（Vitest 31）；`App.tsx` 拆分组件、规则参数化 `standSoft17`/`blackjackPayout`（Vitest 33）；修复 `.dealer-zone` 绝对定位遮挡 `.table-meta` 历史按钮（`z-index: 1`，浏览器实测前后对照）。
- 2026-09-22 v1 浏览器矩阵：Chrome 完整对局/存档导入导出/无效文件/跨标签/双击防护/动画中刷新/弹窗焦点/存储禁用、WebKit 加倍与刷新、中文与 320px、390px 截图，均通过；桌面复验含五个面板开合与 Esc（Kimi 桌面浏览器；文件选择不支持处以代码审查核对 `closePanel` 清空 `pending`）。
- 2026-09-22 规则 v2：开发自测（Vitest 60 项、内置浏览器 320px 中文冒烟，期间修复状态标签与下注区重叠）→ 独立最终验收通过（Chromium 100 / WebKit 53 / Firefox 93，零产品缺陷，含质量门槛复跑全绿），REQ-2026-003–006 归档并合入 main；完整证据与提交前复核已提炼进各归档需求文档的"验收记录"小节。
