# 规则 v2：最终验收交接

日期：2026-09-22。状态：**已结项——最终验收已于 2026-09-22 由独立验收 Agent 完成，结论：通过（零产品缺陷）。** 结论与全部证据见 [v2 验收报告](./v2-acceptance-report.md)；需求已归档，testing/roadmap 已回填。提交、推送、PR 与发布仍按用户后续指令处理。本文件保留为交接过程记录；下文为交接时点的原始状态，未追溯改写。原文：实现与开发自测完成，依用户要求暂停在最终验收前；本文件不是验收通过记录。

## 工作区与范围

- 分支：`codex/plan-split-insurance-surrender`。包含原 M0 文档与本轮实现；尚未提交、推送或创建 PR。
- REQ-2026-003–006 保持 `active`，正文标注“已实现、待最终验收”；最终验收完成后才按流程归档。REQ-001、REQ-002 未在本次处理。
- 已实现：规则 v2 共用状态与逐手结算、最多两手分牌、保险决策与独立赔付、晚投降、v2 存档校验与旧档保护、中英文操作与规则说明、双手展示和每局历史明细。
- 没有迁移 v1、增加多副牌、规则设置界面、多人或云端功能；未发布。

## 实现入口与设计决定

| 需求                                                     | 实现入口                                                                         | 重点                                                                      |
| -------------------------------------------------------- | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| [REQ-003](./archive/REQ-2026-003-rules-v2-foundation.md) | `src/domain/game.ts`、`src/infrastructure/save.ts`、`src/application/useGame.ts` | activeHand、originalBet、逐手状态/结果、保险；version/rulesVersion 均为 2 |
| [REQ-004](./archive/REQ-2026-004-split-hands.md)         | `src/domain/game.ts`、`src/presentation/components/GameTable.tsx`                | 同点值分牌、两手顺序、分 A 特例、分牌 21 非天然                           |
| [REQ-005](./archive/REQ-2026-005-insurance.md)           | 领域 insurance 阶段、`ControlDeck.tsx`                                           | insure/declineInsurance 先于天然检查，保费半注、净赔 2:1                  |
| [REQ-006](./archive/REQ-2026-006-late-surrender.md)      | 领域 surrender 命令、`ControlDeck.tsx`                                           | 原始未行动手晚投降，返还半注且保留保险损益                                |

- 存储键刻意继续使用 `green-room.blackjack.v1`，避免新键绕过旧档检测。负载只接受 v2；`problem: version` 阻止操作与设置写入，明确确认重开/导入后才覆盖。存储失败仍保留会话内存进度。
- `legalActions(state, rules?)` 与 transition 使用同一默认规则。三项规则开关均经参数接线；运行界面和存档只支持默认桌规，6:5/软 17 变体仍是领域测试能力。
- 历史每局一条，包含逐手结果、保险和汇总。`mixed` 表示不同手的不同结果，不按总净收益冒充某手胜负。
- 未新增资金固定上限；若转换金额无法以安全整数精确表示，命令原样返回且不提交事件。正式复核时请关注极大金额边界。
- 设计快照、架构接口说明、v2 存档契约及双语 README 已同步；正式验收前不能宣称 v2 已发布。

## 开发自测证据

2026-09-22 本地执行：

| 检查                   | 开发结果                |
| ---------------------- | ----------------------- |
| `npm test`             | 2 个测试文件，60 项通过 |
| `npm run typecheck`    | 通过                    |
| `npm run format:check` | 通过                    |
| `npm run build`        | 通过，Vite 6.4.3        |
| `git diff --check`     | 通过                    |

测试位于现有 `game.test.ts` 与 `save.test.ts`，覆盖新增规则组合、非法动作、事件快照不变性、独立结算、v1/未知版本拒绝、伪造状态和历史；另有 600 局固定随机种子，在每个决策点验证存档往返与 30 局历史截断。自动测试不替代 UI 和应用协调层最终验收。

浏览器只做开发冒烟：内置浏览器、开发服务、320px 中文，导入固定牌序的分牌夹具，执行分牌及第一手停牌，刷新后仍在第二手行动；页面宽度与 scrollWidth 均为 320。发现并修复状态标签与下注区重叠。未完成生产浏览器矩阵，英文窄屏、保险和投降的完整浏览器流程均待最终验收。

Playwright CLI 因访问 npm registry 时 DNS 失败未能启动；改用内置浏览器进行了上述有限冒烟。既有 Firefox 环境问题仍不得计作通过。临时夹具在已忽略的 `output/playwright/v2-split.json` 和 `v2-insurance.json`，不是发布内容；如缺失可用 `testHelpers.fixedRandom` 重建：

- 分牌前：下注 1000，牌序前缀 `[7, 9, 20, 6, 1, 2]`；分牌后两手为 8+2、8+3，余额 198000；第一手停牌后 activeHand = 1。
- 保险前：下注 1000，牌序前缀 `[7, 0, 20, 5, 1, 2]`；庄家明牌 A、无天然。购买保费 500 后仍可分牌或投降；投降后余额 199000，总净收益 -1000。

## 交接 Agent 的最终验收清单（尚未执行）

- [ ] 独立审阅四条需求，对照每条验收标准与最终代码，补充必要反例；复跑完整质量门槛。
- [ ] 用生产 `dist/` + `npm run preview` 验证分牌同点值、分 A、加倍、混合输赢、双爆牌与整局历史，仅结算一次。
- [ ] 验证保险买入/放弃、余额恰好/不足、双方天然组合；确认购买前暗牌和结果不泄露，重复操作无重复扣款。
- [ ] 验证晚投降可用时机、非法时机、半筹码返还、保险损失保留及双语文案。
- [ ] 在保险决策、第一/第二手、发牌与结算动画中刷新/导出/恢复；验证真实下载和导入替换，以及损坏/伪造/v1/未来版本保持原进度。
- [ ] 验证旧自动存档版本提示、游戏和设置写入阻止、明确确认重开/有效导入，以及跨标签写入冲突、重新载入和存储禁用。
- [ ] 桌面与 320px 中英文检查双手、长手牌横向滚动、状态标签、历史明细、保险按钮、无页面横向溢出；检查原生键盘、弹层焦点与减少动态效果。
- [ ] 记录实际 Chrome/WebKit/Firefox 覆盖与环境阻塞；不可沿用 v1 通过记录或本次有限冒烟宣称 v2 通过。
- [ ] 发现缺陷则修复并复验；通过后回填 testing、更新里程碑/需求状态并归档。提交、推送、PR 和发布按用户后续指令处理。
